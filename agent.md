# ShellReady 项目目标与开发约定

## 项目定位

ShellReady：新服务器，一键配顺手。

面向后端开发者和独立开发者，解决 SSH 登录一台新 Linux 服务器后，需要反复安装和配置终端工具的问题。用户在远程服务器运行一条命令，由脚本完成工具安装和持久配置，后续登录继续使用。

重点是日常命令操作体验与 BBR 配置，不扩展成服务器管理面板、业务部署平台或数据库应用商店。

仓库：https://github.com/9star-academy/ShellReady

## 已确定的需求

- 项目名称为 ShellReady。
- 使用 Bash 脚本，通过 wget 或 curl 下载后执行；一个安装入口，内部按组件拆分模块。
- 在服务器上永久安装和配置，不采用本地连接时临时携带整个 Shell 环境的方案。
- 一次安装以下六个组件，不要求用户分别手动安装和编写配置。

| 组件 | 目标 |
| --- | --- |
| BBR | 默认启用当前内核支持的 BBR，检查实际状态；BBRv3 内核升级为可选功能 |
| trzsz-go | 安装 Go 版本，提供 trz 上传、tsz 下载 |
| Starship | 安装官方 Rust 实现并配置提示符 |
| zsh-autosuggestions | 提供基于历史命令的输入建议 |
| Atuin | 提供命令历史记录与搜索 |
| zoxide | 记住常用目录，提供快捷跳转 |

Zsh 是 zsh-autosuggestions 的依赖，应纳入依赖处理，不算额外的第七个产品功能。使用这些工具不意味着目标服务器必须安装 Go 或 Rust 编译环境。

## 已确定的 BBR 策略

采用方案 1：默认启用现有 BBR，提供独立的可选 BBRv3 内核升级入口。

- 默认一键流程安装其余五个工具，并检测、启用当前内核支持的 BBR；不更换内核，不因 BBR 默认配置要求重启服务器。
- 当前内核不支持 BBR 时，明确报告跳过及原因，继续安装其余工具，不自动转入内核升级，也不报告全部成功。
- BBRv3 内核升级仅在用户主动选择后执行，先检查系统、架构和引导兼容性，保留可用旧内核供回退。
- 升级成功后标记“已安装，待重启验证”；重启是重启整台服务器，不是重新连接 SSH，必须由用户决定，不自动执行。
- 重启后核验当前运行内核和 BBRv3 实际状态，不能把安装包成功等同于新内核已生效。
- 重新连接 SSH 用于加载持久 Shell 配置，不能替代内核升级所需的服务器重启。

本节记录已确认的产品行为；安装器与可选升级代码已实现，实际 Linux 安装、内核启动与回退验收仍需完成。

## 参考项目及借鉴边界

用户明确指定参考 [byJoey/Actions-bbr-v3](https://github.com/byJoey/Actions-bbr-v3)。

- 借鉴其脚本入口、系统和架构识别、版本选择、状态检查与卸载说明的组织方式。
- README 采用清晰的环境说明、组件介绍、使用流程、验证方式和参考链接。
- 参考项目包含 BBRv3 内核安装；“启用当前内核 BBR”和“安装新内核获得 BBRv3”必须分别说明。
- 已确定默认使用现有内核 BBR；参考项目的 BBRv3 内核安装仅用于可选升级设计，不纳入默认一键流程。
- 不直接执行或整段接入上游安装脚本；先审查其修改范围、版本来源及失败行为。
- 如需复用源码或分发二进制，应核对相应版本的许可证并保留必要的上游声明。

## 实现约束

下面的约束适用于后续修改；代码存在不代表实机验收已经通过。

1. 安装前检查系统、架构、权限、依赖和已安装版本。
2. 优先使用固定且经过验证的官方预编译版本，校验下载产物；入口与模块使用同一发行版本。
3. 明确目标用户，避免 sudo 提权后误把 Shell 配置写到 root 用户目录。
4. 保留原配置，使用项目管理的独立文件与可识别接入段；重复运行不重复追加。
5. 每项给出成功、失败或跳过的真实结果；部分失败不能报“全部完成”。
6. 下载使用独立临时目录，仅清理本次创建的文件。
7. Atuin 建议默认仅本机记录，不自动注册账号或开启跨机器同步；历史导入应避免重复。
8. 本地终端必须支持 trzsz，或使用 tssh / trzsz ssh。服务器安装完成不等于已经通过实际传输验证。
9. BBR 是否生效、是否为 v3、内核是否待重启分别检查，不能仅凭算法名 bbr 判定 v3。
10. 可选 BBRv3 内核升级遵循上方已确定策略：保留可用旧内核，检查引导更新结果，不自动重启；重启后再核验。
11. 卸载仅移除本项目管理的内容，保留用户历史记录、原配置和原有工具。
12. 不因上游支持某项功能，就扩大 ShellReady 的默认安装范围。

## 首版实现选择与待办

以下为本次实现采用的范围，后续以实际测试结果维护，不表述为用户已经逐项确认的偏好。

- 远程使用 `bash <(curl -fsSL https://raw.githubusercontent.com/9star-academy/ShellReady/main/bootstrap.sh)`；公开入口可访问后才能使用。bootstrap 下载同一 commit 的完整源码，再调用依赖本地模块的 install.sh。
- 默认一条命令直接安装全部，无交互菜单；支持 install、update、status、uninstall、bbrv3、kernel-remove。
- 安装器允许 Ubuntu 22.04/24.04、Debian 12/13，x86_64/ARM64。尚待 CI 与实机验证。
- 默认为目标用户设置 /bin/zsh，提供 --keep-shell；普通用户调用 sudo 后仍以原用户为目标。
- 配置使用标准 HOME/.zshrc；符号链接配置或自定义 ZDOTDIR 不自动修改。保留现有配置和主题文件。
- 工具装入目标用户 ~/.local/share/shellready，提供本地 shellready 管理命令；固定清单见 config/assets.tsv。
- update 按当前清单修复或安装；不会自动拉取上游 latest，变更清单需使用新版仓库。
- 默认 BBR 不改现有 qdisc；不支持返回跳过，应用失败尝试恢复。
- 可选内核来源为 byJoey/Actions-bbr-v3 标准 release；用户必须提供 tag 和 --yes，按 API SHA-256 校验 linux-image 包。只接受 Ubuntu 24.04、Debian 12/13 和可验证的 GRUB 回退环境。
- 普通卸载保留全机 BBR；--restore-bbr 显式恢复，发生后续修改冲突则停止。独立 kernel-remove 拒绝移除正在运行的内核。
- 当前电脑无 Docker/Linux VM，不得声称 Linux 安装、SSH 传输、内核启动/回退已验证。
- 项目许可证仍待维护者确定；首版已由用户提交，公开下载入口暂返回 HTTP 404，推送及仓库可见性尚未确认。

不把这些未决项变成已确认承诺；也不让它们阻碍文档等独立工作。

## 验证要求

实现脚本后至少覆盖：

- Bash 语法检查、ShellCheck 静态检查。
- 空白环境安装、已有配置环境安装、重复执行、下载失败、权限不足。
- root 与普通 sudo 用户的配置归属和文件权限。
- 重新 SSH 登录后，Starship 显示、命令建议接受、Atuin 搜索、zoxide 跳转。
- 使用兼容客户端完成 trz / tsz 实际上传下载并比对文件。
- 默认 BBR 流程不安装新内核、不重启服务器；不支持时明确跳过且不阻断其他工具。
- BBR 运行状态与重启后的持久性（测试重启由测试人员执行）；可选 BBRv3 升级额外验证用户主动选择、待重启状态、引导和旧内核回退。
- 卸载或恢复不损坏已有环境、不删除历史记录。

容器测试不能代替虚拟机或真实服务器的内核、重启、SSH 交互验证。每次交付说明实际执行的检查及未验证范围。

## 文档与协作

- 当前为首版实现阶段，已有代码与本地测试；完整验证记录见 docs/verification.md。
- README 面向使用者；本文件记录需求和开发边界。
- 未实现功能标记为规划中，不发布指向不存在脚本的可执行安装命令。
- 小范围增量修改，不引入无关框架或依赖，不提交、推送 Git，除非用户明确要求。
- 本文件按用户要求命名为 agent.md。它是项目上下文文档，不假定所有代理会自动加载；需要自动发现时另行配置标准入口。
